# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added
- The non-published `:kalligraphie:bench` module measures the library instead of asserting it. The three former JVM-only opt-in measurements — glyph materialization, incremental paragraph layout and the editable line, including the consumer, session, font-asset handoff and concurrent journeys — become 37 `MeasurementScenario`s whose selection is derived from `:kalligraphie:conformance`'s capability identity, so a platform runs exactly the profiles its declared capabilities can serve and the JVM-only half joins Android and iOS the day `END_TO_END_LAYOUT` lands there. The timed unit, warmup and percentiles come from kotlinx-benchmark (JMH) through a dedicated `jvmBenchmark` compilation, while each scenario keeps publishing the glyphs, lines or operations it actually consumed next to the latency; memory and allocation figures are labelled measured or unavailable, never estimated, and a profile that consumed nothing is refused outright. The same scenario core already compiles for `androidTarget` and `iosSimulatorArm64`, whose harness entries are the next step. The retired `glyphMaterializationMeasurement` and `editableLineMeasurement` tasks, their environment-gated classes and their default test exclusions are gone from `:kalligraphie`; `:kalligraphie:bench:jvmBenchmarkBenchmark` measures all 37 profiles in one opt-in command and writes raw results plus per-scenario counters outside the repository.
- `:kalligraphie:unicode` carries the Unicode Character Database tables a portable analyzer needs, generated from pinned sources by the new `scripts/unicode/` tooling. Nine tables join the hand-committed `UnicodeVerticalOrientation`: the BiDi class and the BD16 bracket pairs, the extended-grapheme break class with `Extended_Pictographic` and the Indic conjunct break, the line-break class, the script with its `Script_Extensions`, and the likely script of a language. All are Unicode 16.0, all searched by hand without allocating, and each states in its KDoc the source file and the SHA-256 it was derived from. `scripts/unicode/ucd.json` pins the URL, the digest and the properties of every payload, `fetch_ucd.py` obtains and verifies them, and `generate_ucd_tables.py --check` proves the committed tables are what those payloads produce — it reproduces `UnicodeVerticalOrientation` byte for byte, which is what fixed the run-table shape and the wrap width. Two shapes of table are emitted and the difference is load-bearing: a **dense** table covers the whole codespace with the source's `@missing` defaults beneath its ranges, while a **sparse** one carries `RANGE_ENDS` so a scalar falling between its ranges answers the property's own default instead of inheriting the range before it. Nothing reads the new tables yet — the analyzer that does, and the capability declaration it enables, are separate work — and they are `internal`, so the module's published surface does not move.
- `scripts/unicode/tests/` covers the acquisition and the generator offline, and the behavioural suite of `:kalligraphie:unicode` pins every table against the Unicode Character Database's own published values: an explicit range, a scalar the `@missing` defaults decide, and a scalar the table omits. As with `scripts/fonts/`, this bookkeeping stays a documented local obligation rather than a CI step.
- `scripts/unicode/PROVENANCE.md` records the exact upstream URL, release, retrieval date and SHA-256 of every payload, with the Unicode License V3 notice the data is distributed under.

- `:kalligraphie:unicode` segments extended grapheme clusters portably, with UAX #29 implemented over the generated tables instead of the JVM's `BreakIterator`. The segmenter keeps exactly the state the boundary rules need — the previous scalar's break class, the length of the run of Regional_Indicators ending at it, whether the text up to it is `Extended_Pictographic Extend*` and whether it ends in the zero-width joiner that follows such a run, and whether it matches `Consonant [Extend Linker]* Linker [Extend Linker]*` — and agrees with the Unicode 16 `GraphemeBreakTest.txt` corpus on every one of its cases. Nothing consumes it yet: it is the segmentation half of the portable analyzer, and it is `internal`.

### Changed
- `:kalligraphie:unicode` runs its Unicode conformance corpora on every test target rather than the JVM alone. The Unicode 16 data moved from `src/jvmTest/resources` to a shared `src/harnessResources`, reachable through the class path on the JVM and on Android — where the module now has host and device test compilations at all, which the convention plugin did not create — and embedded as base64 Kotlin source for the iOS simulator, whose test binary has no class path. All of it through one corpus seam, so the module keeps a single way to read a corpus. The `iosCorpus` task embeds the grapheme corpus only: the two BiDi corpora are 15 MB together, and the engine they exercise is integer arithmetic over this module's own tables, whose behaviour on that target the grapheme corpus and the table spot checks already establish, so they run on the JVM and on Android. The ICU-backed corpus tests stay on the JVM, the only target with ICU. The portable corpus test now executes on four runtimes — the JVM, the Android host, the Android device under ART, and the iOS simulator — while the module's `commonTest` reaches all but the device, which cannot see it in this repository, exactly as in `:kalligraphie:e2e`.
- The non-published `:kalligraphie:e2e` harness no longer hides tooling in its test suite. Regenerating the golden manifest, the catalog matrix, the claims export or the inspection dumps is now a `JavaExec` task driving `GoldenWriterMain` on the test runtime class path, so the suite excludes no class from `check` and carries no environment-variable switch; `updateE2eGolden` and `e2eGoldenDumps` keep their names, and the dump directory is a task parameter (`-Pkalligraphie.e2e.dumps=…`) instead of an environment variable. The last test that spawned a host tool is gone, the exhaustiveness lint points at the moved claims export, and every fixture in the module — journeys included — is read through the injected `FixtureCorpus`, so no test depends on the working directory any more.
- The non-published `:kalligraphie:e2e` harness separates the half of its golden catalog that needs nothing but the portable glyph route from the half that composes text through the paragraph facade, so the portable half is now compiled for every test target instead of the JVM alone. Each supported entry declares a `CatalogRoute`, each renderer declares the same one and the materializer refuses a disagreement, and the harness derives the scenes a platform must verify from that platform's own `:kalligraphie:conformance` capability identity — the ratchet fails when the registry registers a scene its declared capabilities cannot serve, or omits one they imply. Font bytes and the committed harness resources now reach the tests through an injected `FixtureCorpus` rather than the class path, which is what a target without classpath resources can implement. Nothing user-visible changes: the committed manifest, the generated matrix and the claims export are byte-identical, and no scene's fingerprint moves.
, built from upstream revision `4c2aa804671d7276e8a0eb95da07202ead05c843` with NDK `30.0.15729638` for `arm64-v8a` and `x86_64` and published as `org.graphiks:kffi-harfbuzz-android` with its `kffi-harfbuzz-android-native` payload. The shared Android library floor is raised from API 24 to API 28 — a deliberate breaking change for API 24–27 consumers. The public shaping entry points are renamed without aliases: `JvmHarfBuzzShapingBackend` → `HarfBuzzShapingBackend`, `JvmPreparedFontCachePolicy` → `PreparedFontCachePolicy`, and `JvmPreparedFontCacheUsage` → `PreparedFontCacheUsage`, and the shaping binding coordinate becomes the multiplatform `org.graphiks:kffi-harfbuzz`. Android now declares `SHAPING` present in the portable capability matrix; glyphs, clusters, advances, positions, GDEF ligature-caret facts and typed errors are unchanged.
- The iOS target ships a bundled HarfBuzz 14.3.0 shaping backend, built from upstream revision `4c2aa804671d7276e8a0eb95da07202ead05c843` and statically linked through cinterop for `iosArm64` (device) and `iosSimulatorArm64` (simulator), published as `org.graphiks:kffi-harfbuzz-iosarm64` and `org.graphiks:kffi-harfbuzz-iossimulatorarm64`. iOS now declares `SHAPING` present in the portable capability matrix with the `bundled-harfbuzz` profile, so no current platform is left without a bundled shaping backend. Glyphs, clusters, advances, positions, GDEF ligature-caret facts and typed errors are unchanged, and the iOS simulator suite reproduces the frozen JVM-oracle goldens byte for byte.
- `:kalligraphie:raster-cpu` no longer hosts the composition conformance tests or the `rasterDumps` task: `OutlineConformanceTest`, `PaintConformanceTest`, `BitmapConformanceTest`, `ComposedLineDumps`, `GlyphSheetDumps` and the PGM/PPM canvas helper are absorbed by the non-published `:kalligraphie:e2e` module as golden scenes, the opt-in dump moves to `:kalligraphie:e2e:e2eGoldenDumps`, and the `raster-cpu` README plus the bilingual `raster-cpu` guides point at the new module. Fingerprints are unchanged: the migrated scenes reproduce the retired conformance digests and dump bytes byte for byte. The rasterizer itself, its unit tests, the logo seal and the platform font journeys are untouched.
- The portable outline IR gains an additive `GlyphOutlineIR.Command.CubicTo` and `GlyphOutlineCommand.CubicTo` for cubic Bézier segments, plus matching canonicalization, destructuring and non-finite rejection. Recompile consumers with exhaustive `when` handlers over these sealed types; move, line, quadratic and close behaviour is unchanged.
- Bitmap capability discovery is conservative and face-wide: every declared strike of a route must be structurally valid, one malformed unselected strike withdraws the route, and scan budgets for compressed and decoded bytes accumulate across all declared strikes while materialization applies per-strike budgets; duplicate declared strikes are rejected with the route-specific duplicate-strike failure when requested and withdraw the route during capability discovery.
- The canonical portable bitmap profile fingerprint changes: the strike token now carries the exact bit depth, so regenerate persisted profile fingerprints for every bitmap profile, including profiles whose declared parameters did not otherwise change (previously schema-1). Bitmap representation schema version 2 replaces version 1: `BitmapStrike` appends a required `bitDepth`, `BitmapLimits.maxBitmapTableBytes` becomes `maxSourceTableBytes`, and `BitmapProfile` defaults to schema version 2. Recompile consumers that construct these values, pass the exact strike bit depth (1 for the existing EBLC/EBDT monochrome route), and expect `font.unsupported-representation-profile` for version 1 bitmap profiles; existing monochrome glyph results are unchanged.
- Bitmap resource-bound breaches are now terminal instead of representation-local: a `BitmapResourceLimitExceeded` aborts the calling resolution instead of falling back to another accepted profile or font, unlike the generic `ResourceLimitExceeded` still used by the outline, SVG, and paint readers.
- The optional Apple module uses dedicated kffi CoreText bindings and Darwin system-information access instead of declaring native frameworks, symbols and layouts locally; font certification, resource ownership, budgets, cancellation and public leases retain their existing contracts.
- The JVM HarfBuzz shaping backend uses the published `org.graphiks:kffi-harfbuzz-jvm` binding instead of declaring native library loading, symbols, signatures, memory layouts and owners locally. Glyphs, clusters, advances, positions, GDEF ligature-caret facts and typed errors are unchanged; the native distribution provenance now names the kffi artifact while keeping the same binary digest, upstream source revision and build chain.
- macOS directory capture retains existing Kotlin call defaults but appends examination/diagnostic options and cancellation parameters: recompile consumers because the JVM constructor/factory entry points changed. Captures refresh explicitly into isolated generations.
- Font captures accept a trailing optional shared cache scope; recompile JVM consumers using changed factory/constructor signatures. Existing Kotlin calls retain their defaults and CoreText retention stays disabled by default.
- Paint schema 3 appends interpolation parameters to `GlyphPaintColorLine` and `PaintGraphProfile`, and three SVG transport bounds to `PaintGraphLimits`: recompile consumers using the changed JVM constructors or generated copy signatures, handle `PathClip` and the new interpolation enums, and regenerate persisted profile fingerprints even for schema 1 or 2. Historical COLR interpolation defaults are preserved; see the bilingual font-management migration notes.
- Static COLR v1 expands paint constructors, `PaintGraphLimits` generated `copy` signatures and sealed/enum cases: recompile consumers, update exhaustive handlers, never persist enum ordinals, and regenerate persisted profile fingerprints, including schema 1. Ordinary recompiled Kotlin constructor calls retain defaults; this is not JVM binary compatibility. Empty-group rejection now occurs when constructing schema-1 `GlyphPaintIR`, not `Group` alone; see the font-management migration notes.
- FontMaterializationCachePolicy now uses structured per-face/per-catalog budgets: generated Kotlin copy/destructuring operations are source- and binary-incompatible with the former byte-only data class; migrate to perFace/perCatalog and recompile consumers that used the old generated operations. The historical byte-only constructor and getter remain available.
- `FontError.CertificateNotInLayout` extends the sealed error surface used by the new font-asset handoff route. Add an arm when recompiling exhaustive Kotlin `when` handlers; existing method and JVM signatures remain available, but an already compiled exhaustive handler can throw `NoWhenBranchMatchedException` if this new member reaches it. Existing calls do not automatically emit this error.
- Kotlin 2.4.0 → 2.4.10
- Gradle 9.5.0 → 9.6.1
- Added blocking pull request policy checks aligned with `CONTRIBUTING.md`.
- The documentation site now embeds the API reference for the Kalligraphie modules.

### Added
- The golden fingerprint claim is executed by CI on every platform the library ships: `.github/workflows/golden-portability.yml` runs the `:kalligraphie:e2e` verification on the five runner architectures of the JVM artifact (Linux and macOS, x64 and arm64, and Windows x64), in the Android unit-test compilation, on the Android device runtime under ART, and in the iOS simulator test binary — each against the same committed manifest, with no numeric tolerance and no per-platform record. The iOS job also runs the `:kalligraphie:raster-cpu` portable suite, which no workflow executed before.
- The non-published `:kalligraphie:e2e` golden verification executes on Android and iOS, not only on the JVM: the portable half of the catalog now runs in the Android unit-test compilation, on the Android device runtime, and in the iOS simulator test binary, and every one of them compares against the *same* committed fingerprint manifest, byte for byte and with no numeric tolerance. Kotlin/Native has no classpath resources, so the iOS binary reads a corpus the `iosFixtureCorpus` task embeds as base64 Kotlin source while Android reads the same resource directories through the class loader. A scene now declares the manifest key it is certified under, so an entry a platform defers still names the record it defers; the verifier receives that derived set, and a scene that is neither rendered nor excused is still reported stale. The paragraph-authored scenes — composed lines, weight ladder, mosaic — remain on the JVM until a portable Unicode-analysis backend lands, which the capability ratchet will then demand on every target.
- The catalogue verifies that heterogeneous material composes, on a new `COMPOSITION` axis. Its `composition.every-route-mosaic` entry is `Supported` and renders one image out of seven rendering routes: a shaped word in a TrueType outline, the capital A decoded through CFF 1 and through CFF 2, a line whose Greek, Arabic and Devanagari scripts only three faces together cover, one glyph at four design weights, a COLR v0 colour paint and a CBDT bitmap strike — all composed on one colour canvas with a shared left margin, cropped to the ink of every band. The scene asserts its own premises: the heaviest weight must carry more coverage than the lightest, and the composed canvas must carry chroma, so a mosaic that stopped mixing fails instead of pinning a plausible-looking picture. Because such a scene draws on several corpus families, `CatalogEntry` gains `composedOf` and `composedTables`: the entry declares every family it composes and the tables its scene reads in each, the renderer declares every resource path it loads, and the materializer checks the two against each other in both directions — a composed family can neither be hidden nor claimed without being drawn. `CatalogClaims.claimsOf` now exports those per-family claims, so the lint sees exactly which family contributes which table. The one family that was previously outside the claimed perimeter, `noto-sans-jp`, is now referenced by the mosaic's style band: it claims the tables that band reads and excuses `BASE`, `HVAR`, `STAT`, `gasp`, `vhea` and `vmtx` with their reasons. `UnpinnedReason` gains `NO_API_SURFACE` for a request no public entry point can express — `composition.per-span-style` records that a paragraph carries one instance descriptor, so a styled run inside one paragraph is not expressible today — and `GoldenSceneFamily` gains `MOSAIC`. The golden fingerprints of the twenty-two existing scenes are unchanged; the manifest gains one line.
- The variation axis is verified end to end on a real variable font. `test-fixtures/fonts/worksans/` commits Work Sans whole — pinned Google Fonts revision, OFL-1.1, `wght` axis 100–900 with default 400 and nine named instances — with its manifest entry, provenance note and licence text, verified offline by `scripts/fonts/fetch_fonts.py --check --provenance`. The new `variation.wght-ladder` expectation is `Supported`: the scene lays one word out five times, once per weight, each through the paragraph facade at its own design-coordinate instance, and composes the five rows on a single baseline grid into one golden image whose canvas is cropped to the ink of all rows. The scene asserts its own premise — the heaviest row must carry more ink than the lightest — so a variation that stopped reaching the outlines refuses with a typed `e2e.*` failure instead of pinning five identical rows. The entry claims `avar`, `cmap`, `fvar`, `gvar`, `GDEF`, `glyf`, `GPOS`, `GSUB`, `HVAR`, `hmtx` and `loca`, which is what the scene really reads; `STAT` and `gasp` are carried by the family and excused with their reasons in `CatalogClaims`. `UnpinnedReason` gains `READER_NOT_IMPLEMENTED` for a technology the corpus carries and the engine does not read yet — `variation.stat` now says that instead of claiming no STAT font had been acquired — and `CatalogMatrixRenderer` renders every reason explicitly, so a reason added without a label fails the build instead of printing the wrong one. The golden fingerprints of the twenty-one existing scenes are unchanged; the manifest gains one line.
- The `test-fixtures/fonts/` corpus is described and verified locally. `scripts/fonts/corpus.json` is the authority of record for every fixture family — its committed artifacts, each file's pinned source, revision and digest, and the sfnt tables the file really carries — and `scripts/fonts/fetch_fonts.py --check` re-hashes the committed bytes and re-checks the licences and the per-family `PROVENANCE.md` without touching the network, while `--fetch` is the only mode that downloads and it refuses any payload whose digest the manifest does not pin, base64 transport excepted. `scripts/fonts/check_exhaustiveness.py` lints the significant tables a corpus font *carries* against the tables the expectation catalog *claims*, reading the claims the e2e module exports, so a carried table can never be ignored by omission, and `scripts/fonts/tests/` covers the manifest shape, the acquisition transport and the lint's comparison rules. These three checks are local commands a maintainer runs before committing a corpus change, not CI steps: the continuous integration of this repository carries business concepts — the catalog ratchet, the golden fingerprints, the generated-artifact freshness — while the corpus bookkeeping and the exhaustiveness lint stay on the maintainer's machine, as `scripts/fonts/README.md` documents. No step downloads a font; `scripts/fonts/README.md` documents the manifest's fields and the acquisition contract.
- The font-technology expectation catalog is published as a generated, bilingual reference. The non-published `:kalligraphie:e2e` module declares every expectation about every font technology as a `CatalogEntry` on one of eight axes (containers, outlines, metrics, variation, colour, bitmaps, scripts, robustness), each entry carrying its technology, its corpus family and one of four statuses: `Supported`, verified by a golden scene and recording the commit that introduced it; `ExpectedRejection`, a refusal that must never disappear; `NotYet`, which pins today's behaviour with a probe or states why nothing can be observed yet; and `OutOfScope`, with a recorded rationale. An audit ratchet keeps the catalog in step with the materialized scenes, the probes, the pinned frames and the auto-sizing exemptions. `CatalogMatrixRenderer` renders the catalog in English and French as a per-status totals table followed by one table per axis, `./gradlew :kalligraphie:e2e:updateE2eGolden` regenerates the committed `docs/docs/generated/e2e-catalog-matrix.md` and its `e2e-catalog-matrix.fr.md` counterpart in the same command that regenerates the golden fingerprint manifest, and the site navigation links the matrix. The matrix is the only place where the catalog's counts appear, so a status flip cannot leave the documentation lying: a freshness test fails whenever a committed page drifts from the catalog, and the matrix writer itself is excluded from `check` and runs only from `updateE2eGolden`.
- Cross-checked the variable glyph ink **bounds** against HarfBuzz. The republished `org.graphiks:kffi-harfbuzz` binding adds `HarfBuzzFont.glyphExtents` (`hb_font_get_glyph_extents`), exposed at the shaping boundary as an internal `PlatformPreparedFont.extents` seam implemented by the JVM, Android and iOS real backends, and `:kalligraphie:shaping:jvmTest` asserts that our `GlyphMetrics.bounds` equals HarfBuzz's varied ink extents at the same location, reconstructed as `minX = x_bearing`, `minY = y_bearing + height`, `maxX = x_bearing + width`, `maxY = y_bearing` (the extents exclude `gvar` phantom points, matching the engine's ink bounds). The audited `NotoSansJP-VerticalFixture.ttf` glyph `A` reconstructs to `(11, 0, 563, 726)` at the default, `(0, 0, 622, 737)` at normalized `0.55999755859375` and `(-8, 0, 668, 745)` at normalized `1.0`, and `SyntheticVariable-CFF2.otf` guards the CFF2 route at `(0, 0, 100, 200)`/`(0, 0, 100, 250)`/`(0, 0, 100, 300)`. The cross-checked sample is non-composite: instanced **composite** ink bounds remain un-cross-checked (the only composite evidence is scaler-level).
- Published the bilingual end-to-end golden reference: `docs/docs/e2e-golden.md` and its French counterpart document the `:kalligraphie:e2e` module, its golden scene catalog across the five golden families, the canonicalization contract, the versioned fingerprint manifest, the comparison verdicts and the `e2e.*` diagnostics, the opt-in `updateE2eGolden` and `e2eGoldenDumps` commands, and the journey/scene boundary, and record that comparison stays exact byte for byte with no numeric tolerance.
- A non-published `:kalligraphie:e2e` module verifies composed output end to end by golden fingerprints: a portable SHA-256, pixel-format and canonical-image model, a versioned fingerprint manifest (`kalligraphie.golden/v1`) with typed `e2e.*` diagnostics, a fail-closed verifier that checks both the manifest record and the declared scene frame, a blocking verification test wired into `check`, and opt-in `updateE2eGolden` and `e2eGoldenDumps` tasks that regenerate the manifest and write PGM/PPM inspection dumps outside the repository. This phase lands the harness and one smoke outline scene whose digest is pinned to the existing CPU-rasterizer fixture; the public-facade journeys and the raster composition conformance move here in later phases. The module is not published and is absent from the consumer dependency graph.
- Design-coordinate font variation selection on `FontInstanceDescriptor`: `fvar` axes and named instances are read, a selection is normalized through `fvar`+`avar` (version 1), and the normalized axes are rebuilt into `FontInstanceKey.geometry`. `FontFace` gains defaulted `variationAxes`, `namedInstances`, `normalize` and `stat`, and `FontInstance` gains a defaulted `fontMetrics`, so existing implementors keep compiling; `FontInstanceDescriptor` gains an optional `variation` field (a trailing defaulted parameter, so recompile JVM consumers for binary compatibility). An unknown axis fails with `font.variation.unknown-axis`, an out-of-range value is clamped with an informational `font.variation.axis-clamped` diagnostic, a selection that combines design and normalized axes fails with `font.variation.ambiguous-request`, and an `avar` version 2 table fails with `font.variation.unsupported-avar-version`. Additional typed codes: `font.variation.invalid-fvar`, `font.variation.unsupported-fvar-version`, `font.variation.invalid-avar`, `font.variation.not-variable`. Metric variation (`HVAR`/`VVAR`/`MVAR`) now lands (see the metric-variation entry below); variable colour now lands (see the variable-COLR entry below) and synthetic geometry now lands (see the synthetic-geometry entry below). The previous rejection of a non-empty normalized-axis selection at `instantiate` is replaced by acceptance as a low-level identity input. `FontFace.stat()` is added as defaulted surface, but portable `STAT` reading remains deferred and `FontInstance.fontMetrics()` is implemented by the metric-variation entry below, and explicitly default-valued axes are retained in the instance identity (no default-value pruning).
- `gvar` simple-glyph outline instancing on the portable TrueType outline route. `:kalligraphie:font:sfnt` gains the opt-in internal `GvarReader.read(...)` producing an immutable `GvarData` with `GvarData.glyphDeltas(...)`, plus `GvarLimits`, `TupleVariationScalars`, `GvarIup` and `GvarResolvedDeltas` for bounded decoding, per-axis region scalars and phantom-slot-aware TrueType IUP interpolation of untouched points; `:kalligraphie:font:scaler` threads the instance's normalized axes through `GlyfReader` and `PreparedTrueTypeFont` and applies the resolved per-point deltas to simple-glyph coordinates before recomputing bounds; `:kalligraphie:font:core` passes `FontInstanceKey.geometry.normalizedAxes` into outline decoding on every render handle, including `TrueTypeFace`, `ColrV1RenderAssetHandle` and the COLR v0 and SVG-in-OpenType glyf fallbacks. A non-default design selection now changes the simple-glyph outline a portable provider returns, not only the instance identity. Malformed `gvar` data fails with `font.variation.invalid-gvar`, an unsupported table version fails with `font.variation.unsupported-gvar-version`, and `gvar` resource bounds reuse the existing `font.resource-limit-exceeded` failure with the `gvar` table location. Recompile consumers that call the changed internal outline-entry signatures, whose new `normalizedAxes` parameter is trailing and defaulted. Composite-glyph `gvar` deltas and phantom-point deltas are now decoded (see the composite `gvar` entry below); metric variation now lands (see the metric-variation entry below); variable colour now lands (see the variable-COLR entry below); synthetic geometry now lands (see the synthetic-geometry entry below).
- Composite-glyph `gvar` outline instancing and phantom-point delta decoding on the portable TrueType route. `:kalligraphie:font:sfnt` gains `GvarData.compositeGlyphDeltas(...)`, which addresses component placement offsets by component index and does not infer un-referenced components, and exposes the four decoded phantom deltas through `GvarGlyphDeltas.phantomDeltas` as a `GvarPhantomDeltas` value. `:kalligraphie:font:scaler` adds `GlyphVariationPhantoms` and the additive, defaulted `ScalerGlyphOutline.variationPhantoms` (with `horizontalAdvanceDelta` = right minus left and `verticalAdvanceDelta` = top minus bottom), restructures the composite reader into collect/decode/resolve phases so a composite's deltas are decoded once for its full component list, applies each component delta only for `ARGS_ARE_XY_VALUES` components and to the raw offset before the optional scaled-component-offset transform, ignores deltas for point-matched components, and leaves the default instance unchanged (`variationPhantoms` is `null`). This remains additive at call sites; recompile consumers that use `ScalerGlyphOutline.copy`, whose signature gained a trailing defaulted parameter. Metric variation and the `FontInstance.fontMetrics()` surface land in the metric-variation entry below; variable colour lands in the variable-COLR entry below; synthetic geometry lands in the synthetic-geometry entry below.
- CFF2 non-default instancing on the portable outline route. `:kalligraphie:font:sfnt` gains a shared, bounded, cancellable format-1 `ItemVariationStore` evaluator in a new `org.graphiks.kalligraphie.font.sfnt.variation` package — `VariationStoreEvaluator` with `VariationStoreLimits` and `VariationStore` — decoding an OpenType `ItemVariationStore` with `maxRegions`, `maxItemData`, `maxAxes` and `maxSourceBytes` enforced incrementally while decoding, and evaluating per-region scalars at a normalized location. A store whose format is not 1 fails with the new `font.variation.unsupported-store-format`, a truncated store fails with the new `font.variation.truncated-store`, an out-of-range region reference fails with the new `font.variation.invalid-store`, and a breach of the declared bounds reuses `font.resource-limit-exceeded`. `:kalligraphie:font:scaler` refactors `CffVarStore` onto that single evaluator instead of duplicating a second format-1 reader, reads the CFF2 default `vsindex` from the selected Font DICT's Private DICT (operator 22, resolved per glyph through `FDSelect` and defaulting to FD 0), seeds the charstring interpreter's initial `vsindex`, threads the instance's normalized axes into the CFF2 outline route, and extracts the shared axis-order mapping `VariationAxisOrder.orderedNormalizedAxes` now used by both the `gvar` and CFF2 routes. The `fvar` axis tags are read lazily and only when a non-empty location is supplied, so the default path adds no parsing and takes the same call path as before; that lazy read deliberately does not thread a cancellation token because `by lazy` would memoize a `Cancelled` result permanently. The shared evaluator corrects the region rule, so a store that contains a zero-crossing region or an invalid bound ordering changes at the default instance (a deliberate fix, not a regression), and a store that declares zero item-data entries is now accepted rather than rejected. A non-default selection now changes the CFF2 outline a portable provider returns: the audited `SyntheticVariable-CFF2.otf` fixture apex is `200` at the default instance and `300` at normalized `wght = 1.0`. This is additive at call sites: `PreparedTrueTypeFont.decodePortableOutline` and `Cff2Reader.readGlyphOutline` gained trailing defaulted parameters. Metric variation (`HVAR`/`VVAR`/`MVAR`) now lands and CFF2 horizontal metrics vary through `HVAR` (see the metric-variation entry below); variable colour now lands (see the variable-COLR entry below); synthetic geometry now lands (see the synthetic-geometry entry below). `gvar`'s `TupleVariationScalars.scalar` now shares the same region rule through the single internal `VariationRegionAxisFactor` (see the corrected `gvar` region-scalar entry below); before that fix it used the older rectangle rule (zero-crossing interpolation and `0.0` for an invalid bound ordering), which diverged from the ItemVariationStore.
- Variable font metrics on the portable routes. `:kalligraphie:font:sfnt` extends the shared `VariationStoreEvaluator` so a format-1 `ItemVariationStore`'s per-`ItemVariationData` delta rows are decoded and interpolated at a location (the `longWords` bit selects int32/int16 word deltas and the remainder are sign-extended int8), and adds `HvarReader`, `VvarReader` and `MvarReader` beside `GvarReader` in `org.graphiks.kalligraphie.font.sfnt.variation`, plus a shared `DeltaSetIndexMap` reader and `MetricVariationLimits`. `:kalligraphie:font:scaler` threads the instance's normalized axes through `PreparedTrueTypeFont.readGlyphMetrics`/`readVerticalGlyphMetrics` into `MetricsReader`/`VerticalMetricsReader` (trailing defaulted delta parameters), reads `HVAR`/`VVAR`/`MVAR` lazily per prepared face, keys the horizontal deltas on the `USE_MY_METRICS` metrics glyph, falls back to the metrics-source glyph's `gvar` phantom deltas when `HVAR`/`VVAR` are absent, and adds `PreparedTrueTypeFont.readFontMetrics`. `:kalligraphie:font:core` passes `FontInstanceKey.geometry.normalizedAxes` at `metrics`/`verticalMetrics` and implements the previously defaulted `FontInstance.fontMetrics()` on `TrueTypeFontInstance` from `OS/2` (with `hhea` fallback), `post` and `MVAR`. The priority is `HVAR`/`VVAR` then `gvar` phantom points then `hmtx`/`vmtx`; horizontal side bearings are adjusted only when the `HVAR` `lsb`/`rsb` mapping is present (the specification requires a mapping for side-bearing variation, so with none the `hmtx` side bearing is retained, unlike fontTools `varLib.instancer` which recomputes it from the varied outline), and the vertical phantom convention is `verticalAdvanceDelta = topY - bottomY`, re-validated against the OpenType chapter and fontTools. At normalized `wght = 1.0` the audited `NotoSansJP-VerticalFixture.ttf` glyph `A` advances `574 + 86 = 660` (`HVAR`) with an unchanged `lsb = 11`, the vertical advance stays `1000` (no `VVAR`, zero phantom delta), and `fontMetrics()` returns `(880, -120, 0, -125, 50, 543, 733)` from `OS/2`/`post` (no `MVAR`). New typed codes: `font.variation.unsupported-hvar-version`/`unsupported-vvar-version`/`unsupported-mvar-version` and `font.variation.invalid-hvar`/`invalid-vvar`/`invalid-mvar`; the embedded store keeps `unsupported-store-format`/`truncated-store`/`invalid-store`, and bounds breaches reuse `font.resource-limit-exceeded`. This is additive at call sites: the metric readers and `PreparedTrueTypeFont` gained trailing defaulted parameters, and `FontInstance.fontMetrics()` was already declared as defaulted surface. `VariationStoreLimits`'s constructor, `VariationStoreEvaluator.read`'s signature, and the public `PreparedTrueTypeFont.readGlyphMetrics`/`readVerticalGlyphMetrics` gained trailing defaulted parameters: recompile consumers that call the changed JVM constructors, `VariationStoreEvaluator.read`, or those two public methods (source-compatible, not JVM binary-compatible). Native bridges remain default-only; `STAT` reading, `avar` v2, `cvar`, `VARC` instancing (the table is now detected and typed-rejected, see the `VARC` entry below) and the fingerprint-regenerating profile limit fields remain deferred, as does the `maxVariationTableBytes`/`retainedBytes` cache-budget billing of the decoded metric tables. The shaping cross-checks now bind the HarfBuzz **horizontal** metric advance and the vertical advance (see the shaping-at-the-instance entry below); the ink bounds are now cross-checked against HarfBuzz on non-composite glyphs (see the ink-bounds cross-check entry below); instanced composite ink bounds remain un-cross-checked. Coverage remains partial: `VVAR` and `MVAR` are exercised only against synthetic bytes because no real fixture carries them, the composite metrics-source plus `gvar`-phantom (no-`HVAR`) horizontal fallback path is code-correct but still untested, the `HVAR` `lsb`/`rsb` and `VVAR` `tsb`/`bsb` mapping-present paths are reader-level only (so the scaler's non-zero side-bearing delta path is untested), and the synthetic variable CFF2 fixture's `HVAR` store declares no regions and no item deltas, so fontTools confirms its glyph `A` advance stays `1000` at `wght = 1.0` and there is no varied advance to assert through the CFF2 metric route. The default font-wide metrics prefer the `OS/2` `sTypo*` fields over `hhea` and deliberately ignore the CSS `USE_TYPO_METRICS` selection bit, so a font whose `OS/2` and `hhea` disagree returns the `OS/2` values. A present-but-empty delta-set index map means the implicit glyph-index identity rather than delta-set zero, matching HarfBuzz. `MVAR` value records wider than the eight-byte minimum are skipped stride-wise for forward compatibility, and `FDSelect` format 4 remains unsupported. The `gvar`/`ItemVariationStore` region-rule divergence noted in the CFF2 entry is reconciled by the corrected `gvar` region-scalar entry below. The variation-location parameters threaded through `VariationStoreEvaluator.read` and the metric readers should be consolidated into one internal value type; the colour route adds one more (`ColrV1Reader.read(orderedAxes)` plus `PreparedTrueTypeFont.orderedVariationAxes`) and records the consolidation as a follow-up rather than expanding its own scope.
- Variable COLR v1 colour on the portable route. `:kalligraphie:font:sfnt` now captures the COLR v1 header's `VarIndexMap`/`VarStore` offsets, parses the table's format-1 `ItemVariationStore` and shared `DeltaSetIndexMap` through the existing evaluator and reader (no second format-1 parser or index-map reader), adds `ColrV1Variation` to address per-field `VarIndexBase + ordinal` deltas, and applies them in each field's raw fixed-point/integer space — `PaintVarSolid`, the `PaintVar*` gradient paints with their `VarColorLine`, the variable transform paints (`PaintVarTransform`, format 13, via `VarAffine2x3`, and the `PaintVar*` translate/scale/rotate/skew variants via their inline variation fields), and the variable `ClipBox` format 2. `:kalligraphie:font:scaler` exposes `PreparedTrueTypeFont.orderedVariationAxes` and `:kalligraphie:font:core` passes the instance's `FontInstanceKey.geometry.normalizedAxes` into `ColrV1Reader.read`, so a non-default selection changes the resolved `GlyphPaintIR`. A store axis-count mismatch fails with the new `font.variation.invalid-colr`, as does the out-of-range `VarIndexMap`/`VarStore` offset guard on the internal `readColrVariation` entry point — a defensive check for a direct caller, since the integrated `ColrV1Reader.readIndexes` range-checks both header offsets first and reports `font.invalid-font-data`; the embedded store keeps `unsupported-store-format`/`truncated-store`/`invalid-store` and bounds reuse `font.resource-limit-exceeded`. The default instance and non-variable COLR fonts are bit-identical: the store is parsed only when the location is non-zero, and it is parsed once per render asset — the `ColrV1Data` the asset already owns, keyed by the full instance — rather than per face or generation. `ColrV1Reader.readColrVariation` honours a cancellation token but checks it before parsing, and the core preparation path passes none today, mirroring the other non-cancellable lazy table reads; the per-glyph paint walk stays cancellable. Additive at call sites: `ColrV1Reader.read` gained trailing defaulted `orderedAxes`/`variationLimits`/`storeLimits`/cancellation parameters and `ColrV1Data`'s internal constructor gained trailing defaulted fields, so existing Kotlin calls keep compiling; recompile consumers that call the changed JVM signature (source-compatible, not JVM binary-compatible). Known, deliberate limitations: a variable radial gradient whose applied deltas drive a radius below zero fails as invalid font data rather than being clamped (the specification permits a negative radius after deltas; the paint model requires non-negative radii), and a COLR paint with an unrecognized format fails through the unsupported-representation path rather than being ignored as the specification allows, so a font that adds a future paint format fails rather than degrading. The variable clip box evaluates in floating point and rounds outward so the box never shrinks (floor toward -infinity, ceil toward +infinity); the reversal check runs on the rounded integers, so a sub-unit float reversal that rounds outward to a non-reversed box is accepted while a reversal of one unit or more still fails. Native bridges remain default-only; `STAT`, `avar` v2, `cvar`, `VARC` instancing (the table is now detected and typed-rejected, see the `VARC` entry below), the fingerprint-regenerating variation profile fields and the `maxVariationTableBytes`/`retainedBytes` cache billing remain deferred. Coverage: the audited synthetic `KalligraphieVarCOLRv1.ttf` exercises `PaintVarSolid`, `PaintVarLinearGradient` with a `VarColorLine`, `PaintVarTranslate` and variable `ClipBox` format 2 at `wght` 400/650/900; the `PaintVarRadialGradient`/`PaintVarSweepGradient` and `PaintVar*` scale/rotate/skew variants share the same accessors and are pinned against the fontTools field table but are not yet fixture-exercised. The variation-location parameter consolidation remains a recorded follow-up (see the metric entry above).
- `VARC` variable-composite detection on the portable outline routes. `:kalligraphie:font:sfnt` exposes `ParsedTrueTypeFont.hasVarcTable`, derived from the validated table directory, and promotes the shared `variationFailure(code, message, tag)` helper to the internal opt-in surface. `:kalligraphie:font:scaler` checks the flag in `PreparedTrueTypeFont.readGlyphOutline` independently of the face flavour: a `VARC` face read at a non-default normalized location fails with the typed `font.variation.varc-unsupported` (location `VARC`) instead of decoding the static composite (`glyf` for a TrueType base, CFF2 for a CFF2 base) that `VARC` supersedes at a non-default instance, so a CFF2-flavoured face cannot bypass the guard. The default instance — an empty location or an all-zero normalized location — is unchanged, because the static composite coincides with `VARC`'s default; a face without `VARC` and a non-variable face take the unchanged path, and metric, vertical-metric and font-metric reads are untouched because `VARC` carries no advance data (advances come from `hmtx`/`HVAR`) and the internal `GlyfReader.readGlyphOutline` call on the TrueType metric path is a phantom-delta metric read, not an outline. Additive only: the promoted helper only widens visibility, and no existing signature changes. No real `VARC` fixture exists in `test-fixtures/`, so the new scaler coverage builds synthetic `VARC` faces into the existing test-font builder — a TrueType face and a CFF2-and-`VARC` face, the latter asserting the typed failure proves the guard is not flavour-gated — and a control pins the no-false-positive path on the audited `NotoSansJP-VerticalFixture.ttf`.
- Published the bilingual conformance matrix: `docs/docs/conformance-matrix.md` and its French counterpart document the declared portable capability matrix, the public-interface tests that carry the conformance authority, the `conformance.portable-capability-absent` absence diagnostic, the verification commands, and the current limits, and record that no numeric tolerance is required while the portable pipeline produces only bit-identical decoding results.
- The non-published conformance module now enables Android instrumented device tests: a Gradle Managed Device is declared, `androidx.test:runner` is registered in the version catalog, and the public portable facade, cancellation and the Android capability identity are exercised on a real Android runtime.
- A non-published `:kalligraphie:conformance` module announces an explicit portable capability identity per platform and hosts public-interface conformance tests for portable decoding, cancellation, atomicity and capability gating.
- macOS system-font provider: `CoreTextSystemFontCatalog` in the optional `:kalligraphie:platform:apple` module discovers the activated CoreText registry through `kffi-coretext` (families, styles, PostScript names and registered file paths) and captures the registered `.ttf`/`.ttc`/`.otf` bytes into a fresh `coretext-registry` generation. A controlled install/removal observed by a new `open` mints a new generation without invalidating earlier snapshots; cross-generation asset keys are refused.
- Completed the bilingual platform-font capability matrix: a per-provider table (discovery, portable data, platform route, refresh, limitations) for macOS, Linux, Windows, Android and iOS, documenting that the iOS provider rebuilds CoreText tables into a standalone container whose `DSIG` signature becomes stale.
- iOS system-font provider: `IosSystemFontCatalog` in the `:kalligraphie:platform:ios` module discovers the CoreText registry through the platform CoreText bindings and rebuilds each font's `.ttf`/`.ttc`/`.otf` content from its copied tables — iOS sandboxes system font files, so no file path is available — into a fresh `ios-coretext-registry` generation. The rebuilt container carries a recomputed table directory and `head.checkSumAdjustment`; a `DSIG` signature becomes stale and is left for the consumer to ignore. Family and face names come from parsing the captured bytes; a new `open` mints a new generation without invalidating earlier snapshots.
- Android system-font provider: `AndroidSystemFontCatalog` in the optional `:kalligraphie:platform:android` module discovers the platform system font collection through `android.graphics.fonts.SystemFonts` (Android 10+; earlier releases have no supported enumeration route and fail with a typed error instead of scanning unknown paths) and captures the reported `.ttf`/`.ttc`/`.otf` bytes into a fresh `android-platform-fonts` generation. Family and face names come from parsing the captured bytes; a controlled change observed by a new `open` mints a new generation without invalidating earlier snapshots.
- Windows system-font provider: `DirectWriteSystemFontCatalog` in the optional `:kalligraphie:platform:windows` module discovers the activated DirectWrite system font collection through `kffi-directwrite` (families, face names, PostScript names and resolved file paths) and captures the reported `.ttf`/`.ttc`/`.otf` bytes into a fresh `directwrite-registry` generation. A controlled install/removal observed by a new `open` mints a new generation without invalidating earlier snapshots; cross-generation asset keys are refused.
- Linux system-font provider: `FontconfigSystemFontCatalog` in the optional `:kalligraphie:platform:linux` module discovers the activated Fontconfig configuration through `kffi-fontconfig` (families, styles, PostScript names and registered file paths) and captures the registered `.ttf`/`.ttc`/`.otf` bytes into a fresh `fontconfig-registry` generation. A controlled install/removal or configuration change observed by a new `open` mints a new generation without invalidating earlier snapshots; cross-generation asset keys are refused.
- Portable CFF1 (Type 2 charstrings) and CFF2 cubic outline routes. `.otf` with a `CFF ` table and CFF2 faces are parsed and materialized as `ScalerGlyphOutline` cubic contours with bounded points/contours, cancellation and typed refusals; `.otf` is accepted only when it carries a real CFF/CFF2 table. CFF1 supports local/global subroutines with bias, hint and hint-mask operators, the move/line/curve/flex operator families, arithmetic and transient operators, charsets 0/1/2, CID-keyed Font DICTs (`FDArray`/`FDSelect`) and the deprecated `endchar` accent composition through StandardEncoding. CFF2 supports the compact header, FDArray/FDSelect, the ItemVariationStore region scalars and `blend`/`vsindex` evaluated at the instance's normalized axes (default and non-default). OTC collections carrying CFF faces are captured. Audited real-font fixtures cover CFF1 and static CFF2, and a fontTools-generated outline oracle covers CFF1 and the synthetic variable CFF2 fixture.
- sbix version 1 colour strikes with `'png '` graphics (version 1, bit 0 of the flags field set, reserved flag bits zero) selected by the exact `ppem` with no neighbouring-size substitution, origins and advances normalized from design units to strike pixels with round-half-away-from-zero, `'dupe'` records resolved to the referenced glyph's image within the same strike, and advances resolved from `hhea`/`hmtx` without requiring `glyf`; `'jpg '`, `'tiff'`, `'pdf '`, and `'mask'` remain unsupported. When a face certifies both colour routes, CBDT/CBLC is selected deterministically and its failure is final: there is no cross-route fallback.
- CBDT/CBLC colour bitmap strikes (table versions 2.0 and 3.0, index subtable format 1, CBDT image formats 17 and 18) decoded through the bounded PNG subset to straight non-premultiplied `RGBA_8888` in sRGB, selected by the exact `(ppemX, ppemY, 32)` strike with no neighbouring-size substitution; suggested `PLTE` chunks are tolerated, ancillary chunks ignored, glyph metrics must match the embedded image dimensions, and uncompressed 32-bit BGRA data, image format 19, and vertical strikes remain unsupported.
- Straight non-premultiplied `RGBA_8888` bitmap pixels in sRGB alongside the existing `ALPHA_8` format, `FontError.BitmapResourceLimitExceeded` with a machine-readable `BitmapResourceLimit` dimension for every bitmap bound, and a bounded PNG decoder that validates declared dimensions before inflation and refuses decompression bombs, palette, greyscale, sixteen-bit, and interlaced images before publishing any pixels. This adds a member to the sealed `FontError` interface and a case to `BitmapPixelFormat`: recompile consumers with exhaustive handlers, and note that an already compiled exhaustive handler can throw `NoWhenBranchMatchedException` if the new member reaches it.
- Deterministic CPU rasterization for tests and demonstrations through `org.graphiks:raster-cpu`: coverage for outlines and paint paths, composed into non-premultiplied RGBA, normalized `ALPHA_8` strike rendering with an explicit ink, typed refusals with declared bounds, sealed coverage fingerprints on audited fixtures, and an opt-in PGM/PPM demonstration runner with a reproducible manifest. The demonstration runner now also publishes multi-script alphabet sheets (Latin, Greek, Cyrillic, Arabic, Devanagari), color sheets and a normalized EBDT bitmap strike, and real text lines composed through the paragraph facade with shaping, BiDi, and three-font fallback; composed outputs are flipped vertically for readability and every dump is double-rendered for byte-exact determinism. The same module renders the repository logo assets — a knocked-out Amiri `K` badge framed on a 512 × 512 transparent canvas for avatars and favicons, plus the Great Vibes wordmark shaped by the pinned HarfBuzz backend for the README — as light and dark transparent PNGs under `docs/assets/`, sealed by pixel fingerprints and a manifest, and regenerated by the opt-in `:kalligraphie:raster-cpu:renderLogo` task.
- Bounded JVM directory font captures and Linux/macOS system-directory providers, including standalone static TrueType and original-index TTC 1/2 faces, detached original source bytes, partial diagnostics, cooperative cancellation and independent render-resource ownership. Collections must fit their complete face count within the remaining examination budget or are rejected whole before examining a prefix; separate fully examined sources can still form partial catalogs, and the accepted-face cap applies independently after examination. Discovery checks cancellation and path bounds before lazy lookahead. Native activated registries and mobile font journeys remain unsupported. The four-target HarfBuzz JVM CI matrix also runs directory and system-catalog shaping/glyph journeys; bilingual guides document source/copy/cache bounds and whole-source examination.
- Caller-owned shared font retention across portable and eligible CoreText captures, with simultaneous face/capture/scope budgets including reservations, retirement and residual cleanup charges; independent consumer owners survive eviction and scope closure. Bilingual guides and a separate opt-in public-consumer measurement document accounting, bounded admission work and known native resource ownership.
- SVG-in-OpenType painted paths and rectangles may reference a preceding local, exact schema-3 `userSpaceOnUse` `clipPath` with one self-closing static path or sharp-cornered rectangle child. Definition and child transforms compose per reference as `T * C * P`, preserving child-paint spaces under an outer `PathClip` intersection. Authored operations consume the shared source budget once; each generated clip independently obeys node, reference, path, clip, depth, paint-visit, and outline limits without a generated `Transform` charge. Reached validation precedes singular or zero-area clip omission, errors remain typed, and acquisition stays atomic with ordered profile fallback.
- SVG-in-OpenType painted paths and rectangles accept finite unitless or percentage `fill-opacity`, defaulting to opaque and clamped to `0..1`. Translucent solids normalize under a shape `PathClip`; gradients derive immutable per-reference stop opacities without changing reusable definitions. Zero opacity validates reached geometry, paint capabilities and resource bounds before omission; unsupported group/CSS opacity and paint attributes on clip children remain rejected.
- SVG-in-OpenType paths may now reference preceding local `userSpaceOnUse` linear or concentric radial gradients. Path geometry remains under the active group transform `T`; linear paint points use `T * G`, while non-solid radial paint retains the existing single `T * G` transform node. Empty and intrinsically solid definitions keep the established clipped normalization, reference-time reuse remains bounds-independent, and path references to `objectBoundingBox` gradients stay typed unsupported even before solid reduction or singular-transform omission. Existing profile capabilities and paint-graph budgets remain authoritative, and failures publish no partial graph.
- SVG-in-OpenType gradients now additionally accept explicit finite unitless `userSpaceOnUse` coordinates on rectangles. Linear geometry uses `T * G` without a graph transform; non-solid concentric radial geometry retains its absolute circle below one existing `Transform` node and one transform-budget entry carrying `T * G`, while each rectangle clip remains under `T` alone. Rectangle-relative gradients preserve `T * B * G`. Linear `x1`/`y1`/`x2`/`y2` and radial `cx`/`cy`/`r` are required in absolute user space, with omitted radial `fx`/`fy` inheriting the accepted center. Viewport-dependent percentages/defaults and unknown coordinate spaces remain typed unsupported; malformed absolute numbers and negative non-zero radii remain invalid data. Coordinate validation precedes empty, one-stop, and zero-radius reductions, profile fallback remains ordered and all-or-nothing, and reuse resolves geometry under each reference transform independently of rectangle bounds.
- Portable `PaintGraphProfile` schema 3 with explicit linear-sRGB or sRGB space and premultiplied or unpremultiplied alpha interpolation, plus bounded `PathClip` nodes charged against path and clip limits; static COLR remains premultiplied while renderers remain consumer-owned.
- The bounded SVG-in-OpenType subset now accepts static `defs` definitions containing `linearGradient` or concentric `radialGradient` paint servers applied to later `rect` elements. It normalizes strict SVG numbers, unpremultiplied alpha interpolation, complete repeat/reflect periods, reached-stop accounting, and all six bounded SVG 1.1 transform functions: ordered `translate`, `scale`, `rotate(angle)`, `rotate(angle cx cy)`, `skewX(angle)`, `skewY(angle)`, and six-coefficient `matrix` syntax normalize into schema-3 linear or radial paint. Rotation and skew angles use SVG degrees; finite negative and wrapped angles remain valid. Authored rotation centers are preserved, exact quadrant rotations and zero/±45-degree-equivalent skews avoid trigonometric residue, and every complete function call consumes one authored transform operation. Skews use the 180-degree tangent period and preserve positive determinant orientation; exact odd-quarter-turn asymptotes, lexically non-zero angles decoded to zero, non-finite tangents, malformed lists, and numerically unrepresentable composition are invalid data. Gradient transforms compose after object-bounding-box geometry, share the authored SVG transform budget, affect only gradient geometry, and reject singular mappings as unsupported. Singular group transforms contribute no filled path or rectangle paint, while numeric rank loss or determinant-orientation inversion is invalid data. Radial geometry uses an explicit bounded transform, while one-stop and zero-radius cases reduce to solid paint under the rectangle `PathClip`. Schema-1 solid SVG keeps its historical node/depth policy without a paint-visit budget.
- SVG-in-OpenType render assets now accept bounded raw UTF-8 and single-member gzip document transport, publish only immutable portable paint IR, and report typed failures for malformed transport and encoded or decoded byte limits.
- Portable `PaintGraphProfile` schema 2 for static COLR version 1 and CPAL 0/1, including resolved palette and foreground colors, gradients, affine transforms, clips, all composition modes, bounded certification, per-glyph fallback, resource limits, and retained-asset handoff; schema 1 remains compatible with existing COLR version 0 and solid-only SVG-in-OpenType routes.
- Opt-in public font-asset handoff and four-worker resolution measurements, with stage percentiles and an observational Apple M2 Max reference outside functional checks.
- Immutable layouts can now be paired with an explicit closable handle that atomically retains all certified font roots and hands independent assets to delayed renderers.
- Structured portable font materialization cache budgets for estimated retained bytes, decoded pixels, native bytes and native allocations, enforced atomically per face and per catalog with global LRU ordering; preserves the historical byte-only constructor and observable glyph results. Native charges remain zero until a native route owns cacheable resources.
- Reproducible opt-in measurement of cold and warm portable TrueType editor stages for preparation, text mapping, metrics, outlines, and detached render assets.
- Exact region-chain composition through `org.graphiks:kalligraphie`: immutable application-supplied `FlowRegion` geometry with exclusions and bounded refinement, logical lines distributed into geometric `LineFragment` values without restarting BiDi or splitting clusters and inline objects, deterministic fragmentation relaxation diagnostics, identity-attested `FlowContinuation` reuse, and incremental forward rematerialization with explicit unmaterialized coverage.
- Advanced typography and derived content through `org.graphiks:kalligraphie`: typed `GlyphProvenance` for direct, derived, and synthetic glyphs, soft and automatic hyphenation (versioned pattern service with digest-verified `hyph-en-us` data and structured diagnostics), inter-word / inter-character / kashida justification, tab stops with START/END/CENTER/DECIMAL alignment and synthetic leaders, ellipsis truncation (`INLINE_START`, `MIDDLE`, `INLINE_END`) with explicit hidden source ranges, inline objects for `U+FFFC` with caret/selection/hit-testing semantics, and final-glyph recertification after every transform.
- Vertical paragraph writing through `org.graphiks:kalligraphie`: `VERTICAL_RL` and `VERTICAL_LR`, Unicode 16.0 UTR #50 orientation, HarfBuzz top-to-bottom shaping with `vert`/`vrt2`, OpenType `vhea`/`vmtx` metrics with explicit deterministic fallback, physical editing geometry, and incremental reflow parity.
- KMP project template (Android, iOS, Desktop)
- JVM reference editable Unicode lines through `org.graphiks:kalligraphie`: canonical UTF-8/UTF-16 decoding, Unicode analysis, pinned HarfBuzz shaping, exact caret and selection geometry, hit testing, and outline-route certification.
- Clean Architecture / DDD
- JVM-only autonomous embedded TrueType catalog support through `org.graphiks:kalligraphie`, including immutable source/face/instance data, cmap and metrics, design-unit outlines, and detached render assets.
- Incremental real-time paragraph layout on the JVM, with versioned edit deltas, reusable checkpoints, cancellation, editor-journey coverage, and opt-in measurement guidance.
- Maven Central publishing via Vanniktech
- OpenType core and SFNT parsing sources for the JVM font stack.
- COLR/CPAL parsing, glyph scaling, and text shaping sources for the JVM font stack.
- Glyph surfaces, glyph cache primitives, A8 rasterization, and renderer-neutral atlas upload planning.
- Multilingual docs (EN/FR) MkDocs + Dokka
- GitHub templates (issues, PR)
- Code of Conduct, CONTRIBUTING, SECURITY, SUPPORT, CHANGELOG
- The HarfBuzz JVM shaping backend now loads on Windows x64: `kffi-harfbuzz-jvm` bundles
  `kffi/harfbuzz/windows/x64/libharfbuzz.dll` (`org.lwjgl:lwjgl-harfbuzz:3.4.3:natives-windows`,
  upstream revision `9f2f0317…`) and the JVM native loader resolves a normalized `windows`/`x64`
  target. Glyphs, clusters, advances, positions, GDEF ligature-caret facts and typed errors are
  unchanged; the four-target CI matrix gains a `windows-x64` entry.
- Synthetic bold and italic geometry on the portable outline route. `FontGeometryParameters.syntheticBold`/`syntheticItalic` — already part of `FontInstanceKey.geometry` — are now applied: `TrueTypeFace.instantiate` accepts them instead of rejecting the descriptor, and `:kalligraphie:font:glyph` gains the pinned `SyntheticGeometry` (exported through `OutlineMaterializer.materialize`'s two new trailing defaulted flags) which offsets each contour outward by `0.02 em` per side with a signed miter offset (bold) and shears every point about the baseline by `tan 14° = 0.2493280028431807` (italic), italic first, recomputing only the integer `DesignBounds` envelope and preserving every contour, point and command so no `OutlineProfile` limit can be breached. The style does not modify advances, side bearings, `GlyphMetrics.bounds`, `VerticalGlyphMetrics` or `FontMetrics`, so a bold outline can overlap its neighbours (the CSS `synthetic` behaviour). The portable `CFF`/`CFF2`/`glyf` outline route honours the style; acquiring a colour (`COLR` v0/v1 or SVG-in-OpenType `PaintGraphProfile`) or bitmap (`BitmapProfile`) asset on a synthetic instance fails with the new `font.geometry.synthetic-unsupported-route` so an untransformed glyph is never returned silently. `syntheticBold` combined with a face that declares a `wght` axis attaches the informational `font.geometry.synthetic-over-axis` diagnostic, even when the descriptor is otherwise at the default instance; a transformed coordinate outside the `Int` range reuses `font.geometry-overflow`, which stays under its pre-existing hyphenated code while the two new diagnostics use the dotted `font.geometry.synthetic-*` namespace. `SyntheticGeometry.VERSION` pins the amounts: changing either amount changes rendered geometry and must bump that version and the TrueType interpretation version. Additive at call sites: `OutlineMaterializer.materialize` gained two trailing defaulted parameters (internal opt-in API); the default instance takes the unchanged path by identity. The bold handedness is derived once per glyph from the contour with the largest absolute area, so an outer contour grows and an opposite-wound hole shrinks (an earlier revision signed each contour independently, which grew holes); a contour containing more than one `MoveTo` now fails with `font.geometry-overflow` instead of being silently offset at its first subpath. `estimateRenderAssetBytes` is not synthetic-gated, so it can report a size for a colour or bitmap profile that `acquireRenderAsset` will refuse on a synthetic instance — an asymmetry, not a silent-asset path. Native bridges remain default-only; only the CoreText bridge wraps a portable face and it rejects a synthetic descriptor explicitly. A paint-graph/document-level synthetic transform for COLR/SVG and a raster-level transform for bitmaps remain deferred.

- Shaping at the instance location on the HarfBuzz backends. The instance's normalized variation location reaches the prepared HarfBuzz font so GPOS/GSUB/kerning variation is applied at that location, and our horizontal `metrics()` advance at the instance is now cross-checked to equal HarfBuzz's advance at the same location: `:kalligraphie:api`/`:kalligraphie:font:core` expose the instance's tag-keyed axes in `fvar` axis order through a new defaulted `@KalligraphieInternalApi` `FontInstance.normalizedVariationLocation()` (delegating to the existing `PreparedTrueTypeFont.orderedVariationAxes`, so no `fvar` parser is duplicated and `:kalligraphie:shaping` keeps its `api`-only dependency), and `:kalligraphie:shaping` threads that `FloatArray` through `HarfBuzzPlatformBinding.prepare` to the native variation setter (`hb_font_set_var_coords_normalized`, 2.14 fixed point) on JVM and Android. The prepared-font pool was already keyed by the full `FontInstanceKey` (face, interpretation, `layoutSize`, `geometry`), so the location is part of cache identity; an empty location (non-variable face or default instance) never calls the setter and the default path is byte-identical. A non-default location against a binding that cannot apply it fails with the new typed `font.shaping-variation-unsupported` rather than shaping silently unvaried. The republished `org.graphiks:kffi-harfbuzz` binding exposes the setter; iOS has no backend at all. The audited cross-check uses `NotoSansJP-VerticalFixture.ttf`: glyph `A` advances `574` at the default, `622` at `wght = 500` (the non-endpoint `avar` case, normalized `0.55999755859375`) and `660` at `wght = 900` (`HVAR`), and the GPOS `kern` pair `AA` advances `563, 574` at `wght = 100` versus `653, 660` at `wght = 900`, proving GPOS variation. The vertical advance (`verticalMetrics()`) is also cross-checked, through the shaping path's top-to-bottom `yAdvance` at the same location: the audited `NotoSansJP-VerticalFixture.ttf` keeps a constant `1000`, while the synthetic `KalligraphieVarVVAR.ttf` carries a `VVAR` advance-height delta that varies it (`1000`/`1100`/`1200`). The ink **bounds** are also cross-checked at the same location through the republished binding's `hb_font_get_glyph_extents`, on non-composite glyphs (see the ink-bounds cross-check entry below).

- Vertical advance cross-check at the instance location. `:kalligraphie:shaping:jvmTest` cross-checks that our vertical advance equals HarfBuzz's at the same location, through the public shaping path: `"A"` is shaped with `ShapingDirection.TOP_TO_BOTTOM` and `ShapedGlyph.yAdvance` (normalized by `toPhysicalVerticalCoordinate` to a positive downward-positive `advanceHeight`) is compared to `FontInstance.verticalMetrics()`. The audited `NotoSansJP-VerticalFixture.ttf` has a constant `vmtx` advance (`1000` at the default, normalized `wght = 1.0` and design `wght = 500`). A new synthetic fixture, `test-fixtures/fonts/kalligraphie-var-vvar/KalligraphieVarVVAR.ttf`, carries a `VVAR` advance-height mapping whose delta moves the advance with the location (`1000`/`1100`/`1200` at normalized `0.0`/`0.5`/`1.0`; HarfBuzz `-1000`/`-1100`/`-1200`), proving the vertical location transport. This cross-checks the vertical **advance**; the "bounds equal HarfBuzz" half is cross-checked separately through `hb_font_get_glyph_extents` (see the ink-bounds cross-check entry below), on non-composite glyphs.

### Changed
- Replaced the Dokka GFM and Python post-processing pipeline with Dokka for Material for MkDocs.

### Fixed
- The `:kalligraphie:e2e` harness is byte-stable on a Windows checkout. `.gitattributes` pins text files to LF everywhere, because the harness digests committed resources — the hyphenation pattern it verifies by SHA-256, the golden manifest, the claims export — and compares the generated pages byte for byte, all of which a `core.autocrlf` checkout rewrote and broke. The claims export no longer proves itself by handing the document to a host `python3`: a unit test that spawned an interpreter, discovered its name and pinned its encoding was asserting the harness's own plumbing more than the export's value, and it is what failed on the Windows runner. What the export must guarantee — its hand-written escaping survives a hostile reason — belongs with the tool that consumes it.
- The PGM/PPM inspection dumps of `:kalligraphie:e2e` open upright, whichever route produced them. `GoldenImage` now declares the row order of its canonical bytes — `GoldenOrientation.IMAGE` (row zero at the visual top) or `GoldenOrientation.DESIGN` (row zero at the visual bottom, the order the CPU rasterizer returns for a y-up outline) — and the five scenes that hand the rasterizer's own rows to the model declare `DESIGN`: the two CFF capitals, the TrueType capital, the VVAR metrics capital and the COLR v0 paint. `GoldenImageReframer` carries that declaration through an auto-sized frame instead of silently rebuilding its content as image-oriented, and `GoldenDumpWriter` reverses a design-oriented raster exactly once, before compositing RGBA over white, so the same shape reads the same way in every dump. The composed canvases and the normalized bitmap strikes already produce image orientation and are written unchanged, and the reversal is a no-op for them. The golden fingerprints, the committed manifest, the ratchet and the in-repo scene frames are untouched: the change is in the inspection artifact and in the model's declaration, not in the bytes the digests cover.
- Instanced glyph ink bounds on the portable TrueType metric route. `:kalligraphie:font:scaler`'s `PreparedTrueTypeFont.readGlyphMetrics` now feeds `GlyphMetrics.bounds`/`scaledBounds` from the requested glyph's **instanced** outline bbox — the varied `gvar` outline points (phantom points excluded, exactly as the static `glyf` header excludes them), computed through the existing `GlyfReader` bounds and passed to the bounds-taking `MetricsReader` overload — instead of the static, non-varied `glyf` header bbox. The value is memoized per `(glyphId, ordered normalized location)` by a bounded, common-safe copy-on-write cache on the shared per-face decoder, so the paragraph projection path amortizes repeated glyphs; the default instance still reads the static header and the CFF/CFF2 route is unchanged. Because the non-default path now materializes the outline under `metricsOutlineProfile()`, a glyph whose `glyf` header is readable but whose outline exceeds the profile (or is malformed) now fails that `metrics()` call, where the `HVAR`-present path previously succeeded without decoding the outline. The audited `NotoSansJP-VerticalFixture.ttf` glyph `A` = gid 1 now reports `(11, 0, 563, 726)` at the default, `(0, 0, 622, 737)` at `wght = 500` and `(-8, 0, 668, 745)` at `wght = 900`; the CFF2 route is a regression-guarded no-op. The memo is capped at 512 `(glyphId, ordered normalized location)` pairs per face with insertion-ordered eviction, so the retained bounds stay bounded. Because the metric route asks for the bounds on every non-default read, an advance-only `metrics()` caller also pays one outline decode per unique `(glyphId, location)` pair on a cache miss, amortized by the memo across repeated glyphs at one location but not free for the first read of each pair. Known residual gap: un-cross-checked instanced **composite** bounds may differ from HarfBuzz for rotated or skewed components (the engine unions the transformed child *points*; HarfBuzz unions component *bboxes*/instructions), and a header-bearing glyph with zero contours keeps the static header bbox while HarfBuzz may return empty extents; both are deferred with the ink-bounds cross-check. This makes the engine value correct; the ink **bounds** are now cross-checked against HarfBuzz through the republished binding's `hb_font_get_glyph_extents` (see the ink-bounds cross-check entry below), on non-composite glyphs. Instanced composite ink bounds remain un-cross-checked: the audited glyph is simple, and the only composite ink-bounds evidence is scaler-level.
- Corrected the OpenType variation-region axis rule for `gvar` (`cvar`) tuple variation stores so it matches the `ItemVariationStore` and the specification. `:kalligraphie:font:sfnt` extracts one shared internal `VariationRegionAxisFactor`, makes both `TupleVariationScalars.scalar` (the `gvar` route) and `VariationStoreEvaluator` delegate to it, and derives the peak-only tent from `start = min(peak, 0.0)` and `end = max(peak, 0.0)` instead of a second branch. A tuple region that spans zero or whose bounds are mis-ordered now ignores that axis (`1.0`, the OpenType "Algorithm for Interpolation of Instance Values" / fontTools `supportScalar(..., ot = true)` rule) instead of interpolating through zero or returning `0.0`, so an affected simple-glyph outline delta (and its IUP-propagated points), composite component offset and phantom-point delta (hence derived advance and side-bearing variation) is no longer halved or silently dropped. This is the deliberate renderer policy: fontTools' partial `varLib.instancer` drops such tents while the render-time (`ttGlyphSet`) and whole-font (`varLib.mutator`) paths and the specification apply the fallback, and this engine is a renderer. Peak-only and well-formed-region behaviour is unchanged, and no public signature changed.
- SVG paint and clip paths report `ResourceLimitExceeded` when supported geometry exceeds declared contour, point or byte limits, preserving the distinction from unsupported paint capabilities.
- Default snapshot publication version when no workflow version is provided.

### Removed
- The `:shared` KMP template and its CI workflow.

### Built with
- Kotlin 2.4.10, Gradle 9.6.1, AGP 9.0.0
- Koin 4.0.0, Ktor 3.0.3, Compose Multiplatform 1.11.1

# Provenance — KalligraphieVarCOLRv1.ttf

- **Source:** synthetic, generated in memory by `build_variable_colr_v1.py`
  (fontTools 4.65.0). Project-owned; no third-party font data or licence applies.
- **Why:** no variable COLR v1 font was available, and the existing
  `skia-colr-v1/test_glyphs-glyf_colr_1.ttf.b64` has no `fvar` and no
  `VarStore`/`VarIndexMap`. This fixture exercises the COLR v1 variable structures:
  the `ItemVariationStore`, the `DeltaSetIndexMap`, `PaintVarSolid`,
  `PaintVarLinearGradient` with a `VarColorLine`, `PaintVarTranslate`, and a
  variable `ClipBox` format 2.
- **Structure (verified with fontTools 4.65.0 by decompiling the generated bytes):**
  `fvar` one axis `wght` (100/400/900), no `avar`; `COLR` version 1; `ClipList`
  format 1 with one clip on glyph 2 (`linear`), `ClipBox` format 2
  `(100,250,900,950, VarIndexBase 12)`; `COLR.VarIndexMap` `DeltaSetIndexMap`
  format 0 with identity `mapping = [0..17]`; `COLR.VarStore` format-1
  `ItemVariationStore`, one region `(0.0,1.0,1.0)` on `wght`, one `VarData` with
  rows `[0,100,-50,200,25,-25,75,4096,-8192,2048,4096,-8192,10,20,-30,-40,50,-60]`.
  Glyph order `['.notdef','box','linear','solid','moved']`; `cmap`
  `0x41→glyph 2 (linear)`, `0x42→glyph 3 (solid)`, `0x43→glyph 4 (moved)`;
  one CPAL palette (red, blue, green).
- **Oracle (raw-space delta application, fontTools `VarStoreInstancer`):**
  - default (`wght=400`, all scalars 0): `linear` p0 `(100,250)`, p1 `(900,250)`,
    p2 `(100,300)`, stop offsets `0.0,1.0`, alphas `1.0,1.0`, clip `(100,250,900,950)`;
    `solid` opacity `1.0`; `moved` translate `(10,20)`.
  - `wght=650` (scalar 0.5): p0 `(150,225)`, p1 `(1000,262.5)`, p2 `(87.5,337.5)`,
    stop offsets `0.125,1.0625`, alphas `0.75,1.0`, clip `(105,260,885,930)`;
    `solid` opacity `0.75`; `moved` translate `(35,-10)`.
  - `wght=900` (scalar 1.0): p0 `(200,200)`, p1 `(1100,275)`, p2 `(75,375)`,
    stop offsets `0.25,1.125`, alphas `0.5,1.0`, clip `(110,270,870,910)`;
    `solid` opacity `0.5`; `moved` translate `(60,-40)`.
- **Regenerate:** `python3 test-fixtures/fonts/kalligraphie-var-colr/build_variable_colr_v1.py`
  (requires `fonttools==4.65.0`).
- **SHA-256:** `afea84bc83bb5403fa2a1375e6029e0ab4c9436ed19b554668d00937bb0db78b`
  (replace if the generator reports a different value for a different fontTools version).
